name: Notify Telegram 🚀

on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Telegram
        uses: appleboy/telegram-action@master
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          format: markdown
          message: |
            *🔔 YANGLANISH XABARI*
            ------------------------------------
            *📁 Loyiha:* `${{ github.repository }}`
            *🌿 Branch:* `#${{ github.ref_name }}`
            *👤 Muallif:* `${{ github.actor }}`
            
            *📝 Xabar:*
            _${{ github.event.head_commit.message }}_
            
            *📊 Holat:* ✅ Muvaffaqiyatli yuklandi
            *🔗 Havola:* [O'zgarishlarni ko'rish](${{ github.event.head_commit.url }})
            ------------------------------------
            *⏰ Vaqt:* ${{ github.event.head_commit.timestamp }}
